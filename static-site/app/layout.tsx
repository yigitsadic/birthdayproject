export const metadata = {
  title: "Birthday App",
  description: "Send birthday emails to your employees!",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
